services:
  db:
    image: postgres:17.5
    restart: always
    shm_size: 128mb
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  pgdata: